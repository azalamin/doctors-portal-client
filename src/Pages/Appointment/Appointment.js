import React from 'react';
import Footer from '../Shared/Footer';

const Appointment = () => {
    return (
        <section>
            <Footer></Footer>
        </section>
    );
};

export default Appointment;